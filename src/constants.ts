export enum DoOutput {
  Always = 'always',
  IfChanged = 'ifChanged',
}

export enum TokenType {
  Command = 'command',
  Variable = 'variable',
}

export enum UnitType {
  Millimeter = 'millimeter',
  Inch = 'inch',
}

export enum Ternary {
  Yes = 'yes',
  No = 'no',
  Maybe = 'maybe',
}
